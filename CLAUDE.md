# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The working guide is **[AGENTS.md](AGENTS.md)**, shared with every other coding agent and imported below. Edit that file, not this one; keep this one to what only applies to Claude Code.

@AGENTS.md

## Claude Code specifics

- **Verify frontend changes with the `frontend:verify` skill** (`frontend/.claude/skills/verify/SKILL.md`: build, launch, drive in a headless browser), not the generic verify skill. That is how "UI changes verified by driving the app" in AGENTS.md's Definition of done is met here.
