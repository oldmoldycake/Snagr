# Web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0
starlette==1.6.0
python-multipart  # multipart parsing for the reference-upload endpoint

# Data / DB
sqlalchemy==2.0.54
asyncpg
alembic
pgvector
pydantic==2.13.5
pydantic-settings
python-dotenv
email-validator

# Auth
argon2-cffi
pyjwt
authlib<2

# Realtime (SSE)
sse-starlette

# MCP server (POST /api/mcp) — pinned: it pulls ~40 transitive packages
fastmcp==4.0.5

# Outbound HTTP — notification delivery, vision sidecar client, OIDC
httpx

# Dev / test
ruff
pytest
pytest-asyncio
asgi-lifespan

# After `pip install -r requirements.txt`, run `pip freeze > requirements.txt`
# to pin the currently-unpinned lines to exact versions.
