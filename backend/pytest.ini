[pytest]
asyncio_mode = auto
; one event loop for the whole session so the async engine can be shared
; between fixtures and tests
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
